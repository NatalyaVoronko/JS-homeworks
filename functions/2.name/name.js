//2. Фукция опредления имени(если имя  ваше, то привет + имя)
const name = prompt ('Введите имя:');
function checkName (name) {
    if (name = 'Наташа') {
        alert ('Привет, ' + name);
    }
}
checkName(name);
